# 开发日志 — 清管器作业系统

## 1. 管段管理保存逻辑不统一 → 统一为失焦自动保存

**问题**：站点管理修改后失焦自动保存，管段管理却要手动点"保存"按钮，体验不一致。

**方案**：管段管理 `PipelineSegmentList.vue` 所有 `el-input` / `el-input-number` 加 `@blur="saveRow(row)"`，操作列从"保存"按钮改为"删除"按钮。`saveRow` 签名从 `(index)` 改为 `(row)` 直接接收行对象，去掉成功 toast（静默保存，与站点管理一致）。

**涉及文件**：`frontend/src/views/segment/PipelineSegmentList.vue`

---

## 2. 跟踪记录表缺少关键站字段 → 新增 `is_key_station`

**问题**：不是所有站点都需要反馈，只需在收发球站、大型跨越、转折点等设置必须反馈的"关键站"。需要区分关键站和普通站以便过滤预警和强制反馈。

**方案**：

- `tracking_record` 表新增 `is_key_station TINYINT(1) DEFAULT 0` 字段 + 索引
- `TrackingRecord` 实体 / `TrackingRecordVO` / 前端 `types` 同步新增字段
- `TrackingRecordServiceImpl.getByOperation()` 手动映射 VO 时转换 `1 → true`
- 创建作业时默认首尾站为关键站，后续可通过向导自定义

**涉及文件**：`database/init.sql`、`TrackingRecord.java`、`TrackingRecordVO.java`、`TrackingRecordServiceImpl.java`、`frontend/src/types/index.ts`

---

## 3. 预警轮询仅针对关键站

**问题**：`WarningServiceImpl.checkStuckConditions()` 对运行中作业查第一个未到达的跟踪记录，不区分是否关键站。非关键站超时也会报警，与"仅关键站报警"的设计意图不符。

**方案**：遍历跟踪记录时跳过 `isKeyStation ≠ 1` 的记录，只对关键站未到达的情况生成卡阻预警。非关键站即使超时也不报警。

**涉及文件**：`WarningServiceImpl.java` — `checkStuckConditions()`

---

## 4. 预警管理页自动刷新会重置多选

**问题**：预警列表每 10 秒自动 `fetchPage()`，Element Plus 表格数据被整体替换后 `selection-change` 触发置空 `selectedIds`，批量操作的勾选状态丢失。

**方案**：

- `fetchPage()` 开头保存 `selectedIds` 到 `Set`
- 数据加载后 `await nextTick()`，遍历新列表用 `tableRef.toggleRowSelection(row, true)` 逐行恢复选中
- 新增暂停/恢复功能：点击标签切换 `autoRefresh`，暂停时停止自动拉取，方便批量操作

**涉及文件**：`frontend/src/views/warning/WarningList.vue`

---

## 5. 分页当前页码显示为深色方块

**问题**：全局主题 `--el-color-primary: #002EA6`（极深藏蓝），Element Plus 分页组件 `.el-pager li.is-active` 用该色做背景，视觉上近乎黑色方块，与周围浅色元素格格不入。

**方案**：将激活页码背景改为 `--el-color-primary-light-3`（`#1a4fcc`，明显更亮），`prev`/`next` 按钮显式设 `background: transparent` 防止继承。

**涉及文件**：`frontend/src/App.vue` 全局样式

---

## 6. 异常作业删除报 FK 约束错误

**问题**：`tracking_record` 表有自引用外键 `parent_record_id → id`（修正记录指向原始记录）。`rollForward` 后子记录引用父记录，直接 `DELETE FROM tracking_record WHERE operation_id = ?` 触发 `SQLIntegrityConstraintViolationException`。

**方案**：删除前先执行 `UPDATE tracking_record SET parent_record_id = NULL WHERE operation_id = ?` 解除自引用链。使用 `UpdateWrapper.setSql("parent_record_id = NULL")` 而非 `.set(column, null)`，因为后者会被 MyBatis-Plus 默认的 null 跳过策略拦截。

**涉及文件**：`OperationController.java` — `delete()`

---

## 7. 节点反馈后下游预测不更新（延误场景）

**问题**：`rollForward()` 查询下游记录的条件是 `.gt(predictedArrivalTime, actualTime)`。当清管器延误时，`actualTime` 可能比下游原始预测时间更晚，导致下游查询结果为空直接 `return`，预测完全不更新。

**根因分析**（时间线）：

```
上游预测时间 A（10:00） → 下游预测时间 B（11:00）
实际到达时间 = 12:00（延误 2 小时）

.gt(B, 12:00) → FALSE（12:00 > 11:00）
→ downstream 为空 → 直接 return → 下游预测停留在 11:00，永远不会更新
```

**方案**：不再用时间比较判断下游，改为按站点位置判断——查出该操作全部跟踪记录按顺序排列，找到当前站在列表中的位置，之后的记录就是需要重算的下游。

**涉及文件**：`OperationServiceImpl.java` — `rollForward()`

---

## 8. 关键站配置入口 → 创建向导新增第 3 步

**问题**：`is_key_station` 在代码里硬编码为 `首尾站 = 关键站`，运维人员无法自定义中间站为关键站。

**方案**（方案 B — 作业级配置）：

- `Operation` 实体新增 `@TableField(exist = false) List<Long> keyStationIds`（仅创建时用，不持久化到 `operation` 表）
- `OperationServiceImpl` 新增 `buildKeyStationSet()`：前端传了定制集合就用它，否则回退默认首尾规则
- 前端 `OperationCreate.vue` 4 步向导扩为 5 步，新增"标记关键站"界面：
  - 复选框列出收发站之间的所有站点
  - 首尾自动勾选且置灰锁死
  - 中间站可自由勾选
  - 提交时随 `keyStationIds` 数组传到后端

**涉及文件**：`Operation.java`、`OperationServiceImpl.java`、`frontend/src/views/operation/OperationCreate.vue`、`frontend/src/types/index.ts`

---

## 9. 关键站反馈按钮显示逻辑修正

**问题**：`isNextArrival()` 在 `idx === 0` 时直接返回 `true`，导致首个跟踪记录（可能是非关键站）也显示"到达反馈"按钮。非关键站不应有反馈按钮，应只对关键站且前面所有关键站均已反馈时才显示。

**方案**：

```ts
function isNextArrival(r: TrackingRecordVO) {
  if (r.actualArrivalTime) return false
  // 非关键站不显示反馈按钮
  if (!r.isKeyStation) return false
  // 所有前面的关键站必须已反馈
  const idx = tracking.value.findIndex(t => t.id === r.id)
  for (let j = 0; j < idx; j++) {
    if (tracking.value[j].isKeyStation && !tracking.value[j].actualArrivalTime) {
      return false
    }
  }
  return true
}
```

验证（管道 12345，关键站 1/3/5，跟踪记录 2/3/4/5）：

| 站 | 关键? | 反馈按钮? |
|----|-------|-----------|
| 2 | 否 | 无 |
| 3 | 是 | 有（第一个关键站） |
| 4 | 否 | 无 |
| 5 | 是 | 3 反馈后才出现 |

**涉及文件**：`frontend/src/views/operation/OperationDetail.vue`

---

## 经验总结

| 类别 | 教训 |
|------|------|
| **MyBatis-Plus null 更新** | `.set(column, null)` 可能被字段策略拦截，紧要操作优先用 `.setSql("col = NULL")` |
| **自引用 FK 删除** | 删除前必须先解除引用链（置 NULL），顺序是 UPDATE → DELETE |
| **时间条件查询** | 用时间大小判断"未来"不可靠（延误时失效），改用位置/顺序判断 |
| **Element Plus 表格刷新** | 数据替换会清空选中，通过保存 ID + `nextTick` + `toggleRowSelection` 恢复 |
| **主题色搭配** | 极深色作背景在小面积元素上近似黑色，用 `-light-3` 变体更协调 |


