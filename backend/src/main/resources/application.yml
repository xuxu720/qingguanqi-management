server:
  port: 8080

spring:
  datasource:
    #url: jdbc:mysql://localhost:3306/qingguanqi?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    url: jdbc:mysql://localhost:3306/qingguanqi?useSSL=false
    username: root
    password: Hx18756775338
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  # 实体包扫描
  type-aliases-package: com.qingguanqi.entity
  # Mapper XML 位置（暂无 XML，保留）
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    # 控制台输出 SQL
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    # 下划线转驼峰
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
