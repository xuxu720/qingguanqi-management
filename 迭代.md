# 2.0 迭代计划 — 智能对话模块升级

**日期**：2026-06-01

## 目标

用户能仅通过智能对话这一个模块实现所有需求。任何原本需要和其他模块交互的过程，都能在对话页面完成。

## 设计原则

1. **LLM 只做意图识别 + 实体抽取**，返回 JSON。查库、校验、构建回复全部是确定性 Java 代码，减少 token 消耗和出错概率。
2. **自然语言描述**：回复中的字段标签用自然语言（"管线名称"而非 `pipelineName`），给出格式规约和示例。
3. **选择优于输入**：涉及已有数据的字段，提供可点击的选项列表，而非让用户打字。
4. **精确编辑走跳转**：修改已有数据的某个具体值，给出跳转链接引导到专用页面。

## Widget 协议

回复从纯文本升级为"文本 + Widget 数组"。前端根据 widgets 渲染交互卡片。

| Widget | 用途 | 典型场景 |
|--------|------|----------|
| `option_list` | 从已有数据中选择 | 选管线、选站点、选清管器 |
| `form_card` | 收集自由文本/数字字段 | 新建管线、添加清管器 |
| `info_card` | 展示查询结果 | 作业进度、预警列表、查询结果 |
| `nav_card` | 引导跳转到专用页面 | 精确编辑某个值 |

## 新增意图

| 意图 | 触发条件 | 返回 Widget |
|------|----------|------------|
| `QUERY_OPTIONS` | "有哪些管线" "石兰线有哪些站点" | `option_list` + `info_card` |
| `QUERY_WARNING` | "最近有什么预警" | `info_card` × N |
| `ADD_PIG` | "添加一个清管器" | `form_card` |
| `ADD_PIPELINE` | "新建一条管线" | `form_card` |
| `ADD_STATION` | "加个站点" | `option_list`（选管线）→ `form_card` |

## 修改清单

### 后端

- 新增 `Widget` DTO（4 种子类型 + WidgetOption/WidgetField/WidgetAction）
- `AgentReply` 增加 `widgets: List<Widget>` 字段
- `PromptTemplates`：更新 system prompt，新增 6 种意图（QUERY_OPTIONS/QUERY_WARNING/ADD_PIPELINE/ADD_PIG/ADD_STATION/UNKNOWN）
- `AgentService`：
  - `buildFollowUpQuestion()` → 改为 `buildFollowUpText()` + `buildFollowUpWidgets()` 双通道
  - 每个缺失参数按类型分发：管线/站点/清管器走 `option_list`，枚举值走 `option_list`，标量走 `form_card`
  - 新增 `handleQueryOptions()`、`handleQueryWarning()`、`handleAddPig()`、`handleAddPipeline()`、`handleAddStation()`
  - 已有意图（CREATE_OPERATION/NODE_ARRIVAL/QUERY_STATUS）回复附带 `info_card` widget
  - 新增 `field()` 快捷方法、`PARAM_LABELS`/`PARAM_HINTS` 自然语言映射表
  - 注入 `WarningMapper` 支持预警查询
- `pom.xml`：添加 `project.build.sourceEncoding=UTF-8` 解决中文编译问题

### 前端

- `types/index.ts`：新增 `Widget`、`WidgetOption`、`WidgetField`、`WidgetAction` 类型，`AgentReply` 增加 `widgets` 字段
- 新增 `components/chat/ChatWidget.vue`：单文件渲染 4 种 widget（option_list/form_card/info_card/nav_card）
  - form_card 支持 text/number/select/datetime 四种字段类型
  - 事件：select / submit / action / navigate
- `ChatView.vue`：
  - 导入 `ChatWidget` 和 `useRouter`
  - `ChatMessage` 接口新增 `widgets` 字段
  - `send()` 存储响应中的 widgets
  - 新增 `handleWidgetSelect()` / `handleWidgetSubmit()` / `handleWidgetAction()` 事件处理
  - 模板中助理气泡内渲染 `ChatWidget` 组件

### 开发过程记录

#### 1. Widget DTO (Java)
创建 `dto/Widget.java`，使用 Lombok `@Builder` 模式。包含 4 种子类型（option_list / form_card / info_card / nav_card），以及 3 个内嵌子类（WidgetOption / WidgetField / WidgetAction）。

#### 2. AgentReply 扩展
`AgentReply.java` 增加 `List<Widget> widgets` 字段。前端通过此字段获取交互卡片数据。向后兼容：widgets 为 null 时前端只渲染纯文本。

#### 3. System Prompt 更新
`PromptTemplates.java` 的 `buildSystemPrompt()` 扩展意图分类规则，新增：
- QUERY_OPTIONS：查询可用数据，提取 queryTarget（pipeline/station/pig）
- QUERY_WARNING：查询预警，提取 pipelineName（可选）
- ADD_PIPELINE / ADD_PIG / ADD_STATION：数据新增，提取对应参数字段
- JSON params 新增 12 个字段（queryTarget / pigSpec / mediumType / diameter 等）

#### 4. AgentService 重构
整个文件从 648 行扩展到约 730 行。核心改动：
- **自然语言映射**：`PARAM_LABELS` Map 将参数 key 映射为自然语言标签（如 `pipelineName` → "管线名称"）
- **智能追问**：`buildFollowUpWidgets()` 根据缺失参数类型生成对应 widget（option_list 查库 / form_card 收集标量）
- **已有意图增强**：CREATE_OPERATION 回复附带 info_card 显示作业摘要 + "查看详情"按钮；NODE_ARRIVAL 回复附带进度卡片 + "标记到达"按钮；QUERY_STATUS 回复附带进度卡片
- **新意图实现**：QUERY_OPTIONS 支持 pipeline/station/pig 三种查询目标；QUERY_WARNING 返回预警卡片列表；ADD_PIPELINE/ADD_PIG/ADD_STATION 执行新增并返回确认卡片

#### 5. 编译问题修复
- Maven 未配置 UTF-8 导致中文文本块编译失败 → pom.xml 添加 `project.build.sourceEncoding=UTF-8`
- Java switch 表达式/语句兼容性问题 → 部分 switch 改为 if-else 链

#### 6. 前端类型同步
`types/index.ts` 新增 Widget 相关接口，`AgentReply` 增加 `widgets?: Widget[]`。

#### 7. Widget 组件开发
创建 `components/chat/ChatWidget.vue`，单文件处理全部 4 种 widget 类型：
- `option_list`：卡片式选项列表，带 label + description，点击触发 select 事件
- `form_card`：支持 text/number/select/datetime 字段，带必填校验，提交时拼接自然语言文本
- `info_card`：键值对信息展示，可附带操作按钮（navigate / arrival / ack_warning）
- `nav_card`：跳转按钮，点击触发 navigate 事件

#### 8. ChatView 集成
`ChatView.vue` 的助理气泡中嵌入 `ChatWidget` 组件。事件处理：
- `select`：将选项值写入输入框并自动发送
- `submit`：将表单拼接文本写入输入框并自动发送
- `action`：navigate 类型 → router.push；arrival 类型 → 拼接到达反馈文本发送；ack_warning → 跳转预警管理页
- `navigate`：router.push

#### 验证结果
- 后端：`mvn compile` 通过，无错误
- 前端：`vite build` 通过，无错误，所有模块正确打包

#### 9. 用户引导设计 (2026-06-01)

**问题**：新增 5 种意图后，欢迎页和快捷模板只覆盖旧版 3 种意图，用户不知道能做数据查询、数据维护。

**修改**：
- **欢迎文案**：从"描述清管作业需求"改为"自然语言即可完成：创建作业、反馈进度、查询数据、新增记录、查看预警"，并引导输入"帮助"
- **快捷模板**：从 4 个扩展到 9 个，按功能分三组：
  - 作业管理：创建作业 / 节点反馈 / 查询进度
  - 数据查询：查看管线 / 查看清管器 / 查看预警
  - 数据维护：添加清管器 / 新建管线 / 使用帮助
- **"帮助"意图**：系统提示词中 QUERY_OPTIONS 新增 `queryTarget=help` 识别，"帮助""你能做什么""有什么功能"均触发能力总览卡片，展示 7 项功能说明 + 4 个快速入口选项

**涉及文件**：`frontend/src/views/agent/ChatView.vue`、`backend/.../agent/PromptTemplates.java`、`backend/.../agent/AgentService.java`

---
